# Production overlay: pin the image tag and keep the base resource set.
# Edit newTag to match a published release (e.g. 0.8.0).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

images:
  - name: ghcr.io/harborclient/team-hub
    newTag: '0.8.0'
