# API

Plugin APIs are grouped by `hc` namespace. Each page starts with conceptual notes (when present) and then the full method reference.

- [UI](/api/ui) — All `hc.ui.register*` methods require the `ui` permission, return a `Disposable` that unregisters the contribution wh…
- [Actions](/api/actions) — Use `hc.actions.register` for lightweight commands discoverable from the Action menu (type `#`). Requires the `ui` pe…
- [Themes](/api/themes) — Theme packages are plugins. Ship the same `.hcp` layout, require the `ui` permission, declare slots in `contributes.t…
- [Commands](/api/commands) — Command handlers tie together menus, toolbar actions, and context menu items.
- [Storage](/api/storage) — Plugin-scoped persistent storage. Keys are namespaced by plugin `id` in the main process. Requires the `storage` perm…
- [Database](/api/database) — Plugin-scoped SQLite database. Each plugin id gets its own file under HarborClient userData (`plugin-databases/{plugi…
- [FS](/api/fs) — Plugin-scoped filesystem access backed by main-process permission checks and a per-plugin path allowlist. Requires `f…
- [HTTP](/api/http) — HTTP lifecycle events are available in both the renderer and main entries. Requires the `http` permission. Registrati…
- [IPC](/api/ipc) — Renderer ↔ main RPC for the plugin. Requires the `ipc` permission.
- [Host](/api/host) — Typed wrappers for built-in request editor commands and library APIs.
- [Imports](/api/imports) — Register handlers for **File → Import** so plugins can participate in the unified import flow instead of adding separ…
- [MCP](/api/mcp) — Register remote MCP client servers so Harbor's chat agent can discover and call tools from external MCP endpoints ove…
- [Live servers](/api/liveServers) — Create, start, stop, and inspect Harbor Live Servers (loopback static file servers with optional CORS, path aliases,…
- [Live pages](/api/livePages) — Create, update, and delete saved Live Pages (website registry rows with URL, home URL, injection scripts, pre/post re…
- [Live page](/api/livePage) — Read-only handle for the **active** live page tab when the plugin UI is hosted in that context. Prefer `hc.livePages`…
- [AI](/api/ai) — Chat pointers, append-only agent instructions, turn hooks, and copy-to-chat for the AI sidebar.
- [Server](/api/server) — **Requires the `server` permission.**
- [Scripts](/api/scripts) — Programmatic request-script execution from the main entry via `hc.scripts.createContext()`. Requires appropriate scri…
- [pluginId](/api/pluginId) — Stable plugin id from the manifest. Use when namespacing storage keys, CSS theme attributes, or diagnostics.
- [React](/api/react) — Plugins must share the host React instance. HarborClient installs `hc.react` before `activate(hc)` runs. `@harborclie…

## Not extensible

These built-in surfaces are not open to plugin contributions:

- **Open request tab strip** — tabs for unsaved/saved requests in the editor workspace.
- **Native window chrome** — title bar and window controls (menu contributions use the application menu only).
