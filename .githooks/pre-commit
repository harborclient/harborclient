#!/usr/bin/env bash
# Intentionally empty.
#
# The previous @harborclient/sdk `link:` override guard is obsolete now that the
# SDK lives in this monorepo as packages/sdk and is consumed via workspace:*.

exit 0
